"""
# Module - array
            >_  Arrays are more efficient than lists.
"""
from array import *

array_one = array('i', [1, 2, 3, 33])    # i for integer
print(array_one)
print(array_one[0])     # Works similar to the lists...
print((array_one[3]))

