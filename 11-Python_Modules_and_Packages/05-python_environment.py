# python Environment    (pythonenv)

'''
    >_ It's a seprate world from rest of the world.

    >_ As the modules get constantly updated, things can break. To prevent this from happening, we create a virtual enironment or, venv which can have it's own version of modules..

    >_ That's why use an IDE, it does some basic things automatically, it always follows the standard over the industry...

    >_ btw, pip install <pythonenv>

'''
